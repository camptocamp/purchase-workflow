<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="purchase_order_form_inerit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='order_line']//tree//field[@name='price_unit']"
                position="before"
            >
                <field name="transport_qty" optional="hide" />
            </xpath>

            <xpath
                expr="//group[hasclass('oe_subtotal_footer')]/parent::group"
                position="after"
            >
                <group
                    class="oe_right"
                    attrs="{'invisible': [('total_transport_qty', '!=', False)]}"
                >
                    <field name="total_transport_qty" />
                </group>
            </xpath>
        </field>
    </record>
</odoo>
